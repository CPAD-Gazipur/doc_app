import 'package:doc_app/utils/utils.dart';
import 'package:flutter/cupertino.dart';

class DoctorAboutInfoSection extends StatelessWidget {
  const DoctorAboutInfoSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Config().init(context);
    return Row(
      children: [],
    );
  }
}
